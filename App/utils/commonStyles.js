import {Dimensions} from 'react-native';

const windowWidth = Dimensions.get('window').width;
const windowHeight = Dimensions.get('window').height;
const colorBox = {
  white: '#ffffff',
};
export {windowHeight, windowWidth, colorBox};
